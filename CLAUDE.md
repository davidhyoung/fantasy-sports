# CLAUDE.md

This file provides guidance to Claude Code when working with this repository.

> **Keep this file and `memory/project_fantasy_sports.md` up to date** whenever you add routes, handlers, pages, schema changes, or new patterns. Accurate docs prevent wasted effort re-reading the codebase.

## Project Overview

Full-stack fantasy sports web app (multi-sport: NFL, NBA). Go backend + React/Vite/TypeScript frontend + PostgreSQL. Users authenticate via Yahoo OAuth, sync their Yahoo fantasy leagues, and view live rosters, scoreboards, standings, and matchup details.

## Architecture

```
backend/   Go API server (Chi router, pgx/pgxpool, golang-migrate, gorilla/sessions, Yahoo OAuth2)
frontend/  React 18 + Vite 5 + TypeScript + Tailwind CSS + shadcn/ui + TanStack Query v5
```

**Backend layout:**
- `cmd/api/main.go` — entry point; wires router, DB pool, sessions, oauth config, handlers
- `cmd/import/main.go` — CLI tool to download nflverse CSV data and upsert into `nfl_players` / `nfl_player_stats`
- `internal/handlers/` — one file per resource; all handlers are methods on `Handler{db, sessions, oauthConfig, config}`
  - `handlers.go` — Handler struct definition + constructor
  - `respond.go` — JSON response helpers
  - `auth.go` — Login, Callback, Me, Logout
  - `leagues.go` — ListLeagues, GetLeague, CreateLeague (creates a **native** league — settings + teams in one transaction; there is no unauthenticated or Yahoo-backed path through this endpoint, Yahoo leagues arrive only via `/api/sync`), `requireCommissioner()` (shared auth check for native-league mutations)
  - `native_leagues.go` — GetLeagueSettings, UpdateLeagueSettings, CreateLeagueTeam, UpdateLeagueTeam, DeleteLeagueTeam: native-league-only CRUD, all commissioner-gated via `requireCommissioner()`. `UpdateLeagueSettings` (and `CreateLeague` in `leagues.go`) reject a nonzero `DEF` slot via `validateNativeSlots()` — same DEF gap as `league_rosters.go`'s `validSlots`, enforced at the settings layer too so a league can't be configured to want something it can never roster
  - `rollover.go` — RolloverLeague: advances a native league to its next season, strategy selected by `leagues.format` via a plain switch (not an interface — one consumer, two real implementations plus a `keeper` 501 stub isn't enough swap-site benefit to justify one). `rolloverDynasty` carries every contract forward (`years_used++`), releases contracts that hit `years_total` to FA, generates next season's rookie picks, advances `leagues.season` (TEXT, so `strconv.Itoa`). `rolloverRedraft` releases everyone and regenerates a full class. `keeper` format 501s — no native keeper-designation flow exists yet, only the Yahoo-draft-result-shaped `keeper_wishlist`/`keeper_rules`. Idempotency is `lockLeagueAtSeason` — locks the league row and confirms `leagues.season` hasn't moved since the request was computed; deliberately **not** "do picks exist for next season", since picks are legitimately generated ahead of a rollover for trading purposes and that used to cause a false-positive 409 on the very first rollover call whenever picks had already been pre-generated
  - `league_rosters.go` — GetLeagueRosters, AssignLeagueRoster, UpdateLeagueRoster, DropLeagueRoster, GetLeagueFreeAgents: native-league roster + contract management. `AssignLeagueRoster` writes `league_rosters` + `league_contracts` in one transaction (via the shared `assignRosterTx` helper — also used by `draft_picks.go`'s pick-use handler) — every roster row gets a contract row, even a $0 undrafted pickup, since cap space is derived as `budget - SUM(salary)`. `UpdateLeagueRoster` moves a player to another team (a trade) and/or edits contract terms. `GetLeagueFreeAgents` is `nfl_players` minus `league_rosters`, ordered by projection for the league's season, with an optional `?search=` name filter (`ILIKE`) used by the assign-player picker. `validSlots` deliberately excludes `DEF` — nflverse has no team-defense `gsis_id`s, so a DEF roster row would always fail the FK. Also home for three small cross-handler helpers: `leagueTeamIDs` (teams ordered by id — the deterministic stand-in for draft order, since native leagues track no standings), `generateDraftClassTx` (inserts one round-robin draft class, `ON CONFLICT DO NOTHING` so pre-generated future picks don't collide with a later rollover, used by picks/rollover), and `relevantPlayerStats` (per-player season-total projected stats — pass/rush/rec yards+TDs, receptions, FG/PAT made — restricted to whichever of those the league's own scoring weights nonzero; "relevant" means "impacts this league's scoring," not just "exists". Rates come from `nfl_projections`, turned into season totals via `scoring.ProjectionToCanonicalTotals` — the same helper draft-values and weekly scoring use. Both `GetLeagueRosters` and `GetLeagueFreeAgents` attach the result as a `stats: [{stat, value}]` array per player, which is what backs the Players tab's dynamic stat columns). Mutations are commissioner-gated; reads are RequireAuth only
  - `draft_picks.go` — ListLeagueDraftPicks, GenerateLeagueDraftPicks, UseLeagueDraftPick: tradable future draft picks (`league_draft_picks`, migration 000023). `GenerateLeagueDraftPicks` is idempotent — 409s if a class already exists for that season, checked before any insert. `UseLeagueDraftPick` locks the pick row (`FOR UPDATE`), 409s if already used, and spends it via `assignRosterTx` with `acquired_via="draft"`. Both log a `league_transactions` row
  - `trades.go` — CreateLeagueTrade, ListLeagueTransactions: `CreateLeagueTrade` moves any mix of players and picks between teams atomically (each asset locked `FOR UPDATE`, destination validated in-league, no no-op moves, ≥2 teams involved) — single-user model, so the commissioner executes directly with no propose/accept handshake, though every move's from/to team is still logged for a future multi-user flow. `ListLeagueTransactions` is a plain read (no gate) over the same `league_transactions` table picks/trades/rollover all write to — powers the Roster tab's activity log
  - `teams.go` — ListLeagueTeams, GetTeam, GetTeamRoster (with stat period support); includes gsis_id batch lookup for player detail links
  - `players.go` — ListPlayers, CreatePlayer, GetPlayer
  - `scoring.go` — GetLeagueScoreboard, GetLeagueStandings: dispatch on `leagues.source` at the top (`leagueSource()` helper) — Yahoo leagues hit the Yahoo API as before; native leagues branch to `nativeScoreboard`/`nativeStandings`, reading `league_matchups` directly (no separate standings table — standings are a pure aggregation over scored matchup rows). Both response structs carry an additive `team_id` field, populated only on the native path
  - `schedule.go` — GenerateLeagueSchedule, ScoreLeagueWeek: native-league weekly play. `GenerateLeagueSchedule` builds a regular-season round-robin via the standard "circle method" (`roundRobinRounds`, fix one team, rotate the rest each round; odd team counts get a bye), idempotent per season (409 if one exists). `ScoreLeagueWeek` is an **explicit, re-runnable commissioner action, not automatic** — the underlying stats come from a manual `make import-nfl` batch import, never live — that sums each team's currently-starter-slotted players' (`league_rosters.slot NOT IN ('BN','TAXI','IR')`) real stat lines (`nflstats.LoadWeekStats`, a per-week sibling of `LoadSeasonStats`) through `scoring.ScoreWithModifiers` and freezes the result onto `league_matchups`. Regular season only — no playoff bracket/seeding built; `league_matchups.is_playoffs` is reserved for later
  - `yahoo_helpers.go` — `leagueYahooKey()`, `userTokens()`, `newYahooClient()` shared helpers; `leagueSettingsSource()` dispatches on `leagues.source` to resolve a `leaguesettings.Source` (native league_settings row vs. live Yahoo) — the seam that lets `draft_values.go` work identically for either
  - `sync.go` — Sync (Yahoo league+team upsert)
  - `league_players.go` — SearchLeaguePlayers, GetAvailablePlayers
  - `keepers.go` — GetKeeperRules, UpdateKeeperRules, GetLeagueDraftResults, GetLeagueKeepers, GetKeeperSummary, ListTeamKeeperWishlist, AddKeeperWishlist, RemoveKeeperWishlist, SubmitKeepers, UnsubmitKeepers
  - `analysis.go` — GetLeagueRankings: weighted z-score rankings; category weights = CV × FA-scarcity (normalised); adds `position_score` (z-score within position group); includes gsis_id lookup for player detail links. NFL season stats source locally from `nfl_player_stats` via `services/nflstats` (Yahoo only returns ownership + league config); non-season stat types + NBA fall back to Yahoo stats
  - `projections.go` — ListProjections, GetProjectionDetail; serves pre-computed comp-based NFL player projections from nfl_projections table
  - `rankings_public.go` — ListPublicRankings: no-auth projection-based rankings with PPR/Half/Standard format toggle (GET /api/rankings)
  - `nfl_players.go` — GetNFLPlayer (full player detail: metadata + YoY stats + projection); GetNFLPlayerByYahooID (resolves Yahoo key → gsis_id and redirects)
  - `draft_values.go` — GetDraftValues: league-specific auction values (VOR + $ value based on actual roster settings); resolves roster positions + scoring via `services/leaguesettings` (Yahoo or native, transparently), uses `services/scoring` for canonical stat-ID translation and league-aware kicker scoring, and `services/tiers` to group interchangeable players within each position (`tier` on every player)
  - `grades.go` — ListGrades, GetPlayerGrades: real-life player grades (0-100 percentile) from nfl_player_grades table; supports comma-separated position filter (e.g. `?position=RB,WR,TE`)
  - `draft_prep.go` — GetDraftPrep, UpsertDraftPrepPlayer, ReorderDraftPrep: the personal draft board in `draft_prep_players`, scoped to (user, league, season) — interest level (signed −3..+3), custom rank, note, and planned cost
  - `draft_consensus.go` — `loadConsensusValues`: consensus auction value per player, on the league's dollar scale. Prefers imported `metric_type='auction'` rows (rescaled from a 12-team/$200 market pool); otherwise **derives** it by reading our own value curve at the market's median within-position rank — "what our board pays for the slot the market gives him". Same median/within-position conventions as `consensus.go`. See `docs/stats/auction-values.md`
  - `divergences.go` — ListDivergences: projection-vs-consensus rank gaps from `nfl_projection_divergences`, ordered by |delta|, each with situational notes. `loadNotesForPlayers` (shared with `nfl_players.go`) attaches both player-scoped notes and team-scoped ones matched via `nfl_players.team`
- `internal/models/models.go` — shared domain types (User, League, Team, Player, RosterEntry, LeagueSettings)
- `internal/middleware/auth.go` — RequireAuth: reads session, attaches *models.User to ctx
- `internal/yahoo/` — Yahoo Fantasy API client, OAuth config, XML types
  - `client.go` — all API methods + dbTokenSource (auto-refreshes + persists tokens)
  - `oauth.go` — Yahoo OAuth2 endpoint + NewOAuthConfig
  - `types.go` — all XML response structs
- `internal/services/scoring/` — canonical stat-ID vocabulary + Yahoo-stat-ID translation + projection→canonical-total helpers; the single place stat-ID knowledge lives
- `internal/services/tiers/` — groups players whose value is close enough to be interchangeable. One rule: a tier's total spread stays inside a budget of `draftable range ÷ targetTiers` (default 8), so cliffs break tiers automatically and "everyone in a tier is within one budget of everyone else" is literally true. Tiering is **per position** — across positions raw points aren't comparable. See `docs/stats/tiering.md`
- `internal/services/nflstats/` — season-level aggregation of `nfl_player_stats` keyed by gsis_id; replaces Yahoo as the NFL stats source for rankings
- `internal/services/leaguesettings/` — the fantasy-context vocabulary (editable roster slot names, canonical per-stat point values) and a `Source` interface (`RosterPositions`, `ScoringMods`) that resolves it for one league, independent of where the league lives. `YahooSource` wraps the live Yahoo settings/scoring calls; `NativeSource` reads a native league's own `league_settings` row. `leagueSettingsSource()` (`handlers/yahoo_helpers.go`) dispatches between them by `leagues.source`, so `draft_values.go` and (eventually) `analysis.go` don't know or care which one they got. See `.claude/plans/native-leagues.md`
- `internal/db/db.go` — pgxpool connect helper
- `migrations/` — numbered SQL migration files

**Frontend layout:**
- `src/api/client.ts` — all typed API functions + TypeScript interfaces
- `src/api/queryKeys.ts` — all TanStack Query cache keys
- `src/lib/queryClient.ts` — QueryClient config (staleTime: 30s, retry: 1)
- `src/lib/utils.ts` — cn() utility (clsx + tailwind-merge), zScoreIndicator, zScoreColor
- `src/lib/grades.ts` — shared grade display utilities: gradeColorClass, trendIndicator, phaseLabel, phaseColor
- `src/lib/constants.ts` — CURRENT_SEASON (2025), PROJECTION_SEASON (2026)
- `src/pages/` — pages by route; complex pages split into subdirectories:
  - `Home.tsx` — the Leagues home: hero + your-leagues list + "Player Outlooks" signal cards (top consensus divergences with All moves / We're higher / We're lower / Has news chips)
  - `league-detail/` — `index.tsx` + tab components (MyTeamTab [Yahoo only], StandingsTab/NativeStandingsTab, ScoreboardTab/NativeScoreboardTab, PlayersTab/NativePlayersTab, NativeRosterTab [native leagues' single combined page for "my team," "every team," and the team-name-click destination — see below], DraftSection [wraps DraftTab + KeepersTab/NativeDraftPicksTab as sub-sections]) + `components/` (PlayerAssignForm, TradeBuilder, NativeRosterTable, NativeTeamOverview, EditContractForm) + `lib/nativeSlots.ts` (shared `ROSTER_SLOTS`/`BENCH_SLOTS`) + `MyTeamRedirect.tsx` + hooks
  - `team-detail/` — `index.tsx` + `components/` (TeamPanel, RosterTable, MatchupCard) + `hooks/useTeamDetail.ts`. `TeamPanel` (matchup card + roster + period switcher) is shared with the league page's My Team tab; `index.tsx` is just the page header around it. Yahoo-only — `/teams/:id` actually resolves through `TeamDetailRouter.tsx` (`pages/`), which checks the team's league source first; a native team just redirects into its league's Roster tab (`?tab=roster&team=<id>`) instead of rendering anything here, so this page's Yahoo-coupled hook never mounts against one
  - `matchup-detail/` — `index.tsx` + `components/` (CategoryTotalsTable, TeamRosterTable) + hook
  - `player-detail/` — `index.tsx` + `components/GradeCard.tsx` — unified NFL player detail page (metadata, grade card, YoY stats table, projection with PPR/Half/Standard toggle + comps)
  - `projection-detail/` — `index.tsx` + `components/` (CompCard, TrajectoryChart) + hook — legacy detail page (redirects to player-detail)
  - `statistics/` — `index.tsx` + `components/GradesTable.tsx` — the single player-data surface at `/statistics`, with a Projections/Grades view toggle (`?view=` drives it). Reuses `projections/hooks` + `projections/components` and `rankings/hooks`. Projections are fixed to `PROJECTION_SEASON`; the Year control only appears in Grades view, since no season has both
  - `rankings/hooks/useRankings.ts` — grades query, consumed by `statistics/` (the standalone page was absorbed)
  - `projections/` — `hooks/useProjections.ts` + `components/` (ProjectionTable, ConfidenceBadge, UniquenessBadge), consumed by `statistics/`. ProjectionTable shows Consensus/Δ columns (partial coverage, "—" where no consensus data exists)
  - `draft-prep/` — `index.tsx` (top-level `/draft-prep`) + `components/` (DraftBoardTable, TeamPanel, NoteField) + `lib/` (roster.ts, interest.ts) + `hooks/useDraftPrep.ts`. **`DraftBoardTable` is the shared draft board**, used here with prep controls and by league-detail's DraftTab without them
  - `divergences/` — `index.tsx` + `hooks/useDivergences.ts` + `components/DeltaBadge.tsx` — the full divergence table at `/divergences`. No longer in the nav: Home surfaces the top signals and links here for the rest. Single-source divergences are flagged (`1*`) since they're uncorroborated
  - `wiki/` — `index.tsx` + `components/` (Formula, Note, Pipeline) — static reference page at `/wiki` explaining the stat engine (grades, projections, VORP/rankings, auction pricing, tiering, consensus divergence) in prose, formulas and worked examples pulled from `docs/`. No data fetching; content mirrors `docs/algorithm-review.md` + `docs/*.md` + `docs/stats/*.md` and should be updated alongside them. `Note`'s color-coded left edge (pink=fix, blue=context, purple=open/future, amber=caveat) reuses the app's semantic palette; purple is reserved for open/future items per the design system's "projected data only" rule.
- `src/components/ui/` — shadcn/ui components (badge, button, input, table, tabs, provider) + table-helpers (SortableHead, PlayerCell, TeamAvatar, ClickableRow, ZScoreCell, HeaderRow) + hand-rolled primitives with no Radix dependency: `mobile-sheet.tsx` (bottom sheet, mobile) and `dialog.tsx` (centered modal, desktop — assign-player, trade builder, rollover confirm), both zero-animation per the design system
- `src/App.tsx` — router + nav (with active page highlighting) + auth check
- `src/main.tsx` — React root, QueryClientProvider, BrowserRouter
- Vite proxies `/api` and `/auth` → `http://localhost:8080` in dev
- HTTPS via `vite-plugin-basic-ssl` (required for Yahoo OAuth)

## Routes

Frontend (nav has four destinations: Leagues, Draft Prep, Statistics and Wiki):
```
/                                       Leagues home — hero, league list, Player Outlooks
/draft-prep                             Draft Prep — board (ranks, interest scale −3..+3, notes) + dockable team panel
/wiki                                   Stat-engine reference — grades, projections, rankings, auction pricing, tiering, consensus (static, no auth)
/leagues                                → redirect to /
/leagues/{id}                           League detail (My Team/Standings/Scoreboard/Players/Draft)
/leagues/{id}?tab=draft&sub=values|keepers  Draft tab sub-sections (?tab=keepers redirects here)
/leagues/{id}/my-team                   → redirect to /leagues/{id}?tab=my-team
/leagues/{leagueId}/matchup/{week}/{t1}/{t2}
/teams/{id}                             Team roster — Yahoo, or a redirect into the league's Roster tab for native (see TeamDetailRouter)
/statistics?view=projections|grades     Player data — projections + real-life grades
/rankings                               → redirect to /statistics?view=grades
/projections                            → redirect to /statistics?view=projections
/divergences                            Full consensus-divergence table (linked from Home)
/players/{gsisId}                       Player detail
/projections/{gsisId}                   → redirect to /players/{gsisId}
```

API:
```
Public:
  GET  /auth/login, /auth/callback, /auth/logout
  GET  /api/health
  GET  /api/leagues, GET /api/leagues/{id}
  GET  /api/leagues/{id}/teams
  GET  /api/teams/{id}
  GET/POST /api/players, GET /api/players/{id}
  GET  /api/projections?season=&position=&sort=&limit=&offset=
  GET  /api/projections/{gsisId}?season=
  GET  /api/nfl/players/{gsisId}              — full player detail (metadata + YoY stats + projection)
  GET  /api/nfl/players/by-yahoo/{yahooKey}   — resolves Yahoo key → HTTP redirect to /api/nfl/players/{gsisId}
  GET  /api/grades?season=&position=&limit=&offset= — real-life player grades; position supports comma-separated (e.g. RB,WR,TE)
  GET  /api/grades/{gsisId}                  — all seasons of grades for a player
  GET  /api/rankings?season=&format=ppr|half|standard&position=&limit=&offset= — projection-based public rankings (no Yahoo, no login)
  GET  /api/divergences?season=&format=ppr|half_ppr|standard&position=&limit=&offset= — projection vs consensus gaps + situational notes

Protected (RequireAuth):
  GET  /api/auth/me
  POST /api/sync
  POST /api/leagues                                      — creates a native league (settings + teams, one transaction);
                                                             no unauthenticated or Yahoo-backed path through this endpoint
  GET  /api/leagues/{id}/settings                         — native leagues only
  PUT  /api/leagues/{id}/settings                         — commissioner only; replaces slots/scoring/budget/num_teams wholesale
  POST /api/leagues/{id}/teams                            — commissioner only
  PUT  /api/leagues/{id}/teams/{teamId}                   — commissioner only; {name?, claim?} — renames and/or claims/releases the team as the caller's own (native leagues only)
  DELETE /api/leagues/{id}/teams/{teamId}                 — commissioner only
  GET  /api/leagues/{id}/rosters                          — every rostered player + contract terms + per-category projected stats the league scores, native leagues only
  POST /api/leagues/{id}/rosters                          — commissioner only; {gsis_id, team_id, slot, acquired_via, salary, years_total}
                                                             writes the roster row + a contract row together; 409 if already rostered
  PUT  /api/leagues/{id}/rosters/{gsisId}                 — commissioner only; move team (trade), slot, and/or contract terms
  DELETE /api/leagues/{id}/rosters/{gsisId}               — commissioner only; drop to free agency (contract cascades)
  GET  /api/leagues/{id}/free-agents?position=&season=&limit=&offset=&search= — unrostered players in a native league, by projection + per-category projected stats the league scores; search is an optional name filter
  GET  /api/leagues/{id}/picks?season=&team_id=            — tradable future draft picks
  POST /api/leagues/{id}/picks/generate                    — commissioner only; {season?, rounds?}; idempotent (409 if that season's class already exists)
  POST /api/leagues/{id}/picks/{pickId}/use                — commissioner only; {gsis_id, slot?, salary, signed_season?, years_total?}; spends the pick, acquired_via="draft"
  POST /api/leagues/{id}/trades                            — commissioner only; {assets:[{kind:"player"|"pick", gsis_id?, pick_id?, to_team_id}]}; ≥2 assets, ≥2 teams involved
  GET  /api/leagues/{id}/transactions?season=               — recent league activity (draft/trade/rollover), newest first; no gate, matches other native-league reads
  POST /api/leagues/{id}/rollover                          — commissioner only; {to_season?, rounds?}; advances the league per leagues.format (dynasty/redraft implemented, keeper 501s)
  GET  /api/leagues/{id}/scoreboard?week=N                — Yahoo API for Yahoo leagues, league_matchups for native
  POST /api/leagues/{id}/schedule/generate                — commissioner only, native leagues only; {season?, weeks?}; idempotent (409 if a schedule already exists for that season)
  POST /api/leagues/{id}/scoreboard/score?week=N&season=  — commissioner only, native leagues only; freezes that week's matchup points from real stats
  GET  /api/leagues/{id}/standings                        — Yahoo API for Yahoo leagues, aggregated from scored league_matchups for native
  GET  /api/leagues/{id}/players?search=q
  GET  /api/leagues/{id}/players/available?position=&start=&status=
  GET  /api/leagues/{id}/draftresults
  GET  /api/leagues/{id}/keepers
  GET  /api/leagues/{id}/keeper-rules
  PUT  /api/leagues/{id}/keeper-rules
  GET  /api/leagues/{id}/keeper-summary
  GET  /api/leagues/{id}/rankings?stat_type=season
  GET  /api/leagues/{id}/draft-prep?season=              — your personal board: interest, custom ranks, tier overrides, notes
  PUT  /api/leagues/{id}/draft-prep/order?season=        — {"gsis_ids":[...]}; array position becomes the rank
  PUT  /api/leagues/{id}/draft-prep/{gsisId}?season=     — {interest, custom_rank, custom_tier, note, planned_cost}; all-empty deletes the row
       interest = +1 target, -1 avoid (null = no opinion; 0 rejected)
       custom_tier = overrides the algorithm's tier grouping (1-20; null = use the computed tier)
       planned_cost = what you intend to pay (null = not in the team plan)
  GET  /api/leagues/{id}/draft-values?season=&budget=200&teams=12&format=league|ppr|half|standard&slots=QB:1,RB:2,WR:3,TE:1,FLEX:1,SFLEX:1,K:1,DEF:1,BN:6&scoring=pass_yds:0.04,pass_td:4,...
       — every setting the scoring depends on is overridable; omitted ones fall back to the league's real Yahoo settings.
         The response echoes what was used in `settings` (same vocabulary), so clients never reverse-engineer roster slots.
         `scoring` is the pointing system — per-stat point values for the 9 categories with rate columns
         (pass/rush/rec yards+TDs, receptions, FG made, PAT made; FG collapses Yahoo's distance buckets into one
         flat value). Present, it replaces `format` entirely and prices every position — not just kickers — from
         those exact weights; omitted, `format`/the league's own scoring governs as before.
  GET  /api/teams/{id}/roster?stat_type=lastweek|season|today  (or ?week=N)
  GET  /api/teams/{id}/keepers
  POST /api/teams/{id}/keepers/{playerKey}
  DELETE /api/teams/{id}/keepers/{playerKey}
  POST /api/teams/{id}/keepers/submit
  DELETE /api/teams/{id}/keepers/submit
```

## Commands

### Backend (`cd backend`)

```bash
make run           # go run ./cmd/api
make build         # go build -o bin/api ./cmd/api
make test          # go test ./...
make migrate-up    # run migrations up (requires DATABASE_URL env)
make migrate-down  # roll back one migration
make generate      # sqlc generate (regenerates internal/db/queries/)
make import-nfl    # import nflverse data (default: 2020-2024)
make import-nfl ARGS="-from 2015 -to 2024"       # custom year range
make import-nfl ARGS="-rosters-only"              # just player metadata
make import-nfl ARGS="-stats-only"                # just weekly stats
make project-nfl ARGS="-profiles"                # build player season profiles (run after import)
make project-nfl ARGS="-project -season 2025"    # compute comp-based projections
make project-nfl ARGS="-grades"                  # compute real-life player grades (0-100)
make project-nfl ARGS="-all -season 2025"        # profiles + grades + projections
make backtest-nfl ARGS="-from 2015 -to 2024"     # backtest projections across historical seasons
make project-nfl ARGS="-cohort-bias -from 2015 -to 2024 -min-base-ppg 8"   # SIGNED bias by cohort + projection level
make project-nfl ARGS="-cohort-bias -from 2015 -to 2024 -sweep-decay-up 0,0.7,0.9"  # sweep the asymmetric-blend knob
make autotune-nfl ARGS="-from 2015 -to 2024 -train-to 2021"  # auto-tune weights, saves projection_config.json
make project-nfl ARGS="-import-consensus f.json -season 2026" # load external rankings/ADP (see docs/stats/consensus-ensemble.md)
make project-nfl ARGS="-import-notes f.json -season 2026"     # load situational news (injuries, camp battles, etc.)
make project-nfl ARGS="-consensus-diff -season 2026 -format ppr" # diff our projection rank vs consensus (non-mutating)
```

Run a single test:
```bash
go test ./internal/handlers/... -run TestHealthHandler
```

### Frontend (`cd frontend`)

```bash
yarn dev           # Vite dev server on :5173 (proxies /api and /auth to :8080)
yarn build         # tsc + vite build → dist/
yarn lint          # eslint
```

### Full stack (Docker)

```bash
cp .env.example .env
docker compose up          # starts db + backend + frontend
docker compose up db       # just PostgreSQL on :5432
```

## Environment

Copy `.env.example` → `.env`. Required:
- `DATABASE_URL` — e.g. `postgres://fantasy:fantasy@localhost:5432/fantasy_sports`
- `PORT` — defaults to `8080`
- `YAHOO_CLIENT_ID`, `YAHOO_CLIENT_SECRET`, `YAHOO_REDIRECT_URL`
- `SESSION_SECRET` — random 32+ char string

Optional (development only):
- `YAHOO_MOCK=1` — serve synthetic Yahoo data instead of calling the real API, and expose `/auth/mock-login` (a session with no authentication). See the Mock Yahoo mode pattern below. **Never set in a deployed environment.**

## Key Patterns

- **Design system:** the frontend follows `~/Downloads/design_handoff_fantasy_sports_app` — warm near-black neutrals, **one** coral-pink accent for all positive/primary meaning, cool blue for negative/secondary, and purple **reserved strictly for projected/future data** (never use it on actual results). Three fonts, one job each: `font-display` (Space Grotesk) for headings/nav/buttons/table column labels, `font-sans` (Inter) for body, `font-mono` (IBM Plex Mono) for **every** number. No gradients, no shadows, no transitions, no animations (loading spinners excepted), no emoji, no icon set in table headers — direction is a typographic glyph (▲ ▼ — ↑ →). Tokens live in `src/index.css` as HSL triplets consumed via `hsl(var(--x))`; **never** use raw Tailwind palette classes (`text-red-600`, `bg-green-100`) — every color goes through a token. Dark is the designed theme; light is a derived inversion. Migration plan and remaining phases: `.claude/plans/design-system-migration.md`
- **Handler pattern:** all handlers are methods on `*Handler`; use `r.Context().Value(models.UserContextKey)` for current user in protected routes
- **Yahoo client:** instantiate per-request via `yahoo.NewClient(ctx, db, oauthConfig, userID, accessToken, refreshToken, expiry)`; tokens auto-refresh and persist to DB
- **Mock Yahoo mode (`YAHOO_MOCK=1`, dev only):** Yahoo's API has rejected this app since ~March 2026 (`403 This application is not authorized` — an app-registration problem outside this repo), which blocks every league route *and* login itself, since `Callback` resolves the user GUID via the Fantasy API. Mock mode swaps the `http.RoundTripper` inside `yahoo.NewMockClient()` (`internal/yahoo/mock.go`) rather than stubbing the 18 client methods — one interception point covers every endpoint, and the real `decode()` path still runs, so the fixtures are continuously validated against the production XML types. Fixtures are Go structs marshalled with `xml.Marshal` (`internal/yahoo/mockdata.go`), never hand-written XML, so they can't drift from `types.go`. The league is a deterministic fixed-seed 12-team snake draft over a committed static player pool (`internal/yahoo/mockplayers.go`, generated from the top ~200 by 2026 projected PPR) — package `yahoo` deliberately has no DB dependency, so mock mode works against an empty database. `/auth/mock-login` is registered **only** when the flag is set (not merely guarded inside the handler) and re-checks the flag; `/auth/login` redirects to it so the existing UI button works with no frontend change. The mock league seeds itself into `leagues`/`teams` through the normal `POST /api/sync` path — no migration, no fixture rows in schema. Mock leagues are identifiable by their `mock.l.` key prefix. **Mock mode applies to every league, not just the mock one:** `newYahooClient` returns `yahoo.NewMockClient()` unconditionally under the flag, so a real, previously-synced league (a genuine `461.l.` Yahoo key from before the March 2026 API breakage) hits the same canned fixtures as `mock.l.100001` for anything that re-fetches from Yahoo — there's no per-league branching. `mockSettingsContent()`'s scoring fixture (fixed 2026-08-18) originally listed a `K` roster slot but only 8 stat categories (pass/rush/rec yards+TDs, INT, receptions) — no FG or PAT stat IDs (19–23, 29) at all. `draft_values.go`'s kicker branch scores strictly off the league's own stat modifiers, so with none defined for FG/PAT, every kicker scored exactly 0 real points (plus stray noise from unrelated categories like rush yards leaking through profile-mean contamination) and priced at $1 regardless of the projection engine underneath — this held even after the `growthRatioPPR` projection fix above, since the auction math never saw any kicker points to price in the first place. Fixed by adding all 6 categories with realistic default modifiers (FG distance-bucketed 3/3/3/4/5, PAT flat 1).
- **Stat type:** GetTeamRoster passes statType directly to Yahoo's semicolon-path syntax (`type=week`, `type=lastweek`, `type=date;date=YYYY-MM-DD`, etc.)
- **Concurrent fetching:** use buffered channels to fan-out Yahoo API calls in GetTeamRoster, GetLeagueDraftResults, GetLeagueRankings
- **Dynamic stat columns:** frontend derives column headers from `roster[].stats[].label` — no hardcoded stat IDs
- **sort_order:** Yahoo's `sort_order` field ("1" = higher is better, "0" = lower is better) is passed through to the frontend for correct winner determination in MatchupDetail
- **Player rankings:** `analysis.go` concurrently fetches rosters, scoring categories, and top-25 FA stats. Category weights = `CV × scarcity` (normalised): CV = stdev/|mean|; scarcity = `1/(1 + max(0, avgFAz))`. `overall_score` = weighted z-sum. `position_score` = z-score within position group (independent). `RankedPlayer` response includes `position_score` + `position_rank`. Frontend: TeamDetail RosterTable shows Value column (`+8.3 #4` overall, `PG #2` position) + color-coded stat cells. See `docs/ranking-algorithm.md`
- **nflverse data import:** `cmd/import/` downloads roster + player_stats CSVs from nflverse GitHub releases, upserts into `nfl_players` and `nfl_player_stats`. Idempotent (ON CONFLICT upserts). Data available from 1999–present; default imports 2020–2024. `nfl_players.yahoo_id` links to Yahoo fantasy player keys for cross-referencing. `nfl_players.team` is normalized through `teamAliases` (`cmd/import/main.go`) for franchises whose nflverse roster files vary the abbreviation across import vintages without the team having moved (ARI/AZ/ARZ, BAL/BLT, CLE/CLV, HOU/HST) — deliberately excludes real relocation history (SD→LAC, OAK→LV, STL/SL→LA), which stays era-specific. Left unnormalized, this silently splits one team's roster across two codes wherever code joins on `team` (it broke team-scoped situational-note matching for Arizona rookies, fixed 2026-08-14).
- **Comp-based projections:** `cmd/projections/main.go` is a batch CLI tool. Step 1 (`-profiles`) aggregates `nfl_player_stats` into per-player, per-season profiles: weekly production is SOS-adjusted (capped defense-vs-position factor), small-sample rates are Bayesian-shrunk toward position-group means (`shrinkage.go`), then z-scored (JSONB). Step 2 (`-project`) blends the target's base-season profile with their immediately preceding season (`recency_blend.go`, games-weighted × `TargetBlendDecay`, `0`=no-op — `docs/stats/recency-weighted-profiles.md`), finds similar historical players (similarity ≥ 0.60, not a fixed count) using weighted Euclidean distance over **dimension groups** (passing/rushing/receiving/value/physical/context/grade), then applies their post-match growth (weighted by `similarity²`), shrunk toward an age/position growth-rate baseline when comps are scarce (`growth_baseline.go`, `GrowthShrinkageK`, `0`=no-op — `docs/stats/bayesian-shrinkage.md` growth-rate extension). Comps that washed out of the league contribute zero growth (survivorship-bias guard); zero-comp players regress halfway to the position mean. Output includes an outcome distribution (`proj_fpts_ppr_stdev/_p10/_p50/_p90`), which stays consistent with the shrunk point estimate since both flow through the same weighted-sum machinery. Draft capital only used for players with < 3 years experience. See `docs/projection-algorithm.md` + `docs/algorithm-review.md` + `docs/stats/`.
- **Short-season shrinkage (`short_season_shrinkage.go`, fixed 2026-08-14):** `recency_blend.go` only softens a short base season by blending it with the immediately preceding one — a rookie (or anyone's first tracked season) whose debut was itself cut short (injury) has no prior season to blend against, so `blendTargetProfile`'s `prior == nil` branch used to hand the partial season to comp matching and growth projection completely unregressed. Case in point: Cam Skattebo's 2025 rookie year ended in a Week 8 injury after a hot 3-TD workload stretch; his 8-game per-game rate got treated as an established full-season role, comping him to RB6 (279 pts) against a consensus market read of RB18 — the largest divergence in the 2026 dataset before the fix (`docs/algorithm-review.md` §6's "injury-shortened base seasons" item). `shrinkShortSeasonTarget` now regresses the *production* fields (fpts, yards, TDs, receptions — the counting stats where TD variance and hot streaks live) and their z-scores toward the position-group mean, weighted by `games_played/17`, whenever there's no prior season to lean on instead; raw opportunity counts (`rush_att_pg`, `targets_pg`) and usage shares stay untouched, matching `shrinkage.go`'s existing "role is real even in small samples" rule. A full season is an exact no-op. Post-fix, Skattebo moved to RB17 (206 pts), matching consensus within one rank. Backtesting (2015–2024) showed the intended trade-off: per-game RMSE/rank-correlation — the tuner's actual objective — improved across every position, while season-total RMSE rose somewhat, consistent with a minority of short-season rookies whose hot stretch really was predictive and are now under-projected; monitor rather than chase further without more signal.
- **Kicker growth ratio (`main.go`'s `computeWeightedProjection`, fixed 2026-08-18):** nflverse's `fantasy_points`/`fantasy_points_ppr` columns never score kicking, so `fpts_ppr_pg` is 0 for essentially every kicker-season in `nfl_player_season_profiles` — any nonzero value (one 2025 kicker had 0.035) is a data quirk, not signal. `growthRatioPPR` (the ratio the engine uses to scale every position-specific rate — pass/rush/rec yards+TDs and, until this fix, FG/PAT made — off projected-vs-current PPR points) divided two near-zero numbers for kickers whenever a base season was short: `shrinkShortSeasonTarget` blends `target.FptsPPRPG` toward the K group mean (itself pulled off zero by that one outlier), while the projected side regresses halfway to that same mean — algebraically, for any games-played weight `w < 1` the ratio works out to `(1 − 0.5w)/(1 − w)`, which blows past `maxGrowthCap` (3.0) for nearly every kicker whose base season wasn't a full 17 games. The result: FG/PAT-made rates tripled for most kickers (Harrison Butker projected at 6.1 FG/game, actually 2.4), pushing projected points past `draft_values.go`'s 180-point kicker sanity cap and zeroing them out — auction values collapsed to $1 for nearly the whole position, with only full-season kickers (never short-season-shrunk, so `growthRatioPPR` stayed at its 1.0 default) landing at a real price. Fix: `growthRatioPPR` is forced to 1.0 for `PositionGroup == "K"` — kicker FG/PAT rates come directly from the (recency-blended / short-season-shrunk) target profile, never scaled by a stat that was never tracking them. Post-fix, kicker point totals landed in a normal 100–170 range with zero hitting the sanity cap. Requires rerunning `make project-nfl ARGS="-project -season <N>"` to take effect — the fix changes projection math, not stored data, so existing `nfl_projections` rows stay wrong until regenerated.
- **Rookie projections (`rookies.go`):** the main comp engine grows a target's *own* base-season profile forward — a player with zero NFL games has no profile to grow from, so incoming rookies (`nfl_players.entry_year == targetSeason`, no `nfl_player_season_profiles` row) are invisible to it. `computeRookieProjections` runs as an additional step inside `-project`: it comps a rookie against historical rookies (`years_exp == 0` profiles) at the same position, weighted by a Gaussian kernel on **draft slot only** (σ=40 picks; undrafted players are pinned to a synthetic slot past the last pick, so UDFA comps against UDFA), and uses those comps' own rookie-season output *directly* as the projection — there's no "current value" of the target's to apply a growth rate to. Always keeps at least `rookieMinComps` (5) comps even if none clear the similarity bar (thin pools, e.g. rookie kickers, are rare in the historical data). `ConfDataQuality` is pinned to 0 (the target has zero NFL seasons of their own), which pulls overall confidence below a comparable veteran's. `base_season` is stored as `targetSeason - 1` for schema consistency only — no profile backs it. Shares `nfl_projections` with veteran projections (same upsert path, same calibration), so every consumer (`/api/rankings`, draft-values, statistics) sees rookies with no query changes. Requires the current season's roster already imported (`make import-nfl ARGS="-from N -to N -rosters-only"`) so `entry_year`/`draft_number` are populated — nflverse publishes each year's roster (including that year's draft class) well before Week 1, independent of stats availability.
- **Consensus divergence (diagnostic, non-mutating):** `cmd/projections/consensus.go` imports external rankings/ADP (`nfl_consensus_rankings`) and situational news like injuries/camp battles (`nfl_player_situational_notes`) from hand-curated JSON files (several major ranking sites block live scraping — see the entry's tradeoffs), resolves players via `sleeper_id`/`espn_id`/name+team fallback, and computes the gap between our projection rank and the **median** external rank/ADP, **ranked within position group** (QB vs QB, RB vs RB — ranking overall conflated raw point volume, which structurally favors QBs, with consensus draft value, which is scarcity-adjusted), into `nfl_projection_divergences` (`-consensus-diff`). Only `standard`/`half_ppr`/`ppr` are diffable — dynasty/superflex consensus data is stored but not compared, since our engine has no multi-year or QB-premium output to diff it against. Situational notes carry a `scope` (`player`|`team`) — team-scoped notes (QB competitions, scheme changes) print alongside every teammate's divergence line automatically, with no inference about which way the impact cuts for that specific player; that judgment stays with whoever reads the report. Nothing here changes `nfl_projections`; it's a review/diagnostic layer pending a season of retrospective validation. A full investigation of the first run's largest divergences (Justin Jefferson, Derrick Henry, etc.) found six overlapping root-cause clusters, not 36 separate bugs — see `docs/algorithm-review.md` §6 for the backlog of genuine algorithm questions it surfaced (injury-shortened base seasons, thin comp pools for rare veteran profiles, team-context blindness on trades). See `docs/stats/consensus-ensemble.md`.
- **Projection calibration:** `cmd/projections/calibration.go` scales every projected quantity by a per-position factor (`projection_calibration` in `projection_config.json`), applied at the end of `computeWeightedProjection` so production and backtest share it. Seeded **uniform at 0.884**, which removed a measured +1.58 ppg (~12%) systematic over-projection: bias +1.583 → −0.000, MAE 3.337 → 2.988 across 2015–2024. A *uniform* factor cancels out of the draft math — `VOR = k·p − k·r = k·(p − r)` and prices are shares of that pool — so it moved **0 position ranks, 0 auction values, 0 consensus values** on the live board while scaling displayed points by 0.884×. Per-position factors (QB 0.953, RB 0.840, WR 0.873, TE 0.880) would calibrate each position exactly but reprice positions against each other; that's a deliberate open decision, not an oversight. Rates are scaled alongside points so yardage still reconciles with points and kickers (scored from rates) don't drift. See `docs/algorithm-review.md` §7.8.
- **Cohort bias (`-cohort-bias`):** `cmd/projections/cohort_bias.go` reports **signed** projection bias split by whether the base season rose/fell/stayed flat, and by projected level. Exists because `nfl_backtest_results` stores only unsigned metrics (RMSE/MAE/correlation), which cannot distinguish a noisy model from a skewed one. Its August 2026 run (`docs/algorithm-review.md` §7.7) refuted the breakout-regression hypothesis and found instead a **proportional ~11–12% global over-projection** — which cancels out of ranks and auction values but inflates displayed point totals. `TargetBlendDecayUp` was added to test asymmetric regression and **stays at 0**: it shrinks risers in a model that is uniformly hot, i.e. right metric, wrong instrument.
- **Projection config:** `cmd/projections/backtest.go` defines `projConfig` (similarity threshold, age window, aging multipliers, per-position dimension-group weights — keyed by group name). Reads `projection_config.json` if present (stale per-stat-keyed configs are detected and ignored). Auto-tuner (`-autotune`) does coordinate ascent maximizing **per-game Spearman rank correlation** and only keeps the tuned config if it beats defaults on held-out validation seasons. `-backtest` runs temporal-integrity backtesting (prior-season data only; shared `projectSeasonBacktest` helper mirrors production) and stores metrics in `nfl_backtest_results` with `eval_basis` 'total'/'per_game' + quantile calibration coverage. Pre-June-2026 backtest rows measured a persistence baseline (bug) and are not comparable.
- **Player detail:** `GET /api/nfl/players/{gsisId}` returns metadata + year-over-year season stats + projection (if exists) + `notes` (situational context, rendered by `SituationalNotes.tsx` below the GradeCard; team-scoped notes are visually labeled so team news isn't misread as player-specific). Player rows in all tables (rosters, rankings, matchup, players tab, draft tab) are clickable and navigate to `/players/:gsisId`. Yahoo→GSIS lookup via `nfl_players.yahoo_id`; batch ANY() query for rosters, per-request map for rankings.
- **Draft tab:** League detail's "Draft" tab (`DraftSection.tsx`) groups everything draft-related into two sub-sections selected by `?sub=`: **Draft Values** (NFL only) and **Keepers** — keeper picks are draft picks, so they live together. Non-NFL leagues show Keepers alone with no sub-tab switcher; the legacy `?tab=keepers` URL rewrites to `?tab=draft&sub=keepers`. Draft Values uses `GET /api/leagues/{id}/draft-values` for league-specific VOR + auction values based on actual roster settings (superflex-aware); position filter only — it **reads** whatever settings were saved on `/draft-prep` (via `readSettings`) but can't edit them, and links there. Its target season is `league.season + 1` **clamped to `PROJECTION_SEASON`** — a league already sitting on the upcoming season (the mock dev league is seeded at 2026) would otherwise request a season with no projections. `GetDraftValues` normalises its nil slices to `[]` so an empty season returns `players: []`, not `null`. `/projections/:gsisId` routes redirect to `/players/:gsisId`.
- **Auction pricing (`draft_values.go` step 7, fixed 2026-08-14):** dollar values are VOR's share of the budget *surplus* — `teams × budget` minus $1 reserved for every roster spot in the league (starters + bench), matching the standard VBD convention the feature is meant to follow (`docs/stats/auction-values.md`); the original version skipped that reservation and split the full nominal budget across only the VOR-positive players, systematically overpricing the whole board (worst at the top). VOR itself is raised to the **0.75 power** before sharing (`auctionVORCompressionExponent`) — real bidders don't execute linear VOR math, and a single-QB league with few flex-eligible starter slots (Yahoo's common RB2/WR2/FLEX1 default) concentrates linear-shared VOR onto so few players that the top of the board blew past $100 in a 12-team/$200 league, well above the observed real-world ceiling (~$70-80 for the consensus #1 overall). The exponent is a single global constant picked empirically against this app's own board, not tuned per league — flagged as an open tradeoff in the doc. Both fixes flow straight through to `consensus_auction_value` too, since the derived consensus price just reads our own curve at the market's rank.
- **Draft Prep page (`/draft-prep`):** the pre-draft surface. Owns everything editable about a draft board; the league page's Draft tab is the same board **read-only**, so there's one place to change things and no chance of the two disagreeing.
  - **Our price vs the market's.** The board carries `Cons $` (consensus auction value) and `Edge` (ours − theirs) columns, prep-page only (`showConsensus`). Uncovered players show `—`, never `$0`, and sort last either way — external sources cover roughly the top 100 picks (62 of 471 players in the 2026 data). Single-source values are flagged `*`. See `docs/stats/auction-values.md`; mechanism in `handlers/draft_consensus.go`.
  - **League picker** (NFL leagues only — the projection engine has no NBA equivalent), remembered in `localStorage` under `fs.draft-prep.league`. A league's roster and scoring are what turn projections into draft values, so the page needs one even though everything about it can then be overridden.
  - **Personal board** (`hooks/useDraftPrep.ts` → `draft_prep_players`): **target (+1) / avoid (−1)**, a custom ranking, a **tier override**, a note, and a **planned cost** per player, scoped to (user, league, season). It went tags → six-level scale (000018) → binary (000019): the finer grades cost more to maintain than they paid back, since deciding between "love" and "like" is a judgment you can't act on differently. The **sign** is kept rather than a boolean so the board can sort on it and so widening the scale again is a constraint change, not a schema one. `NULL` is the only way to say "no opinion"; **0 is rejected** so there aren't two representations of it. The board control is one thumb each way; re-clicking clears. Server-side, not `localStorage` — this is the board you rely on during a live draft, so it has to survive a different browser. Writes are optimistic with rollback, since tagging players during prep should feel instant.
  - **Tier override** (`custom_tier`, migration 000020): the Tier column (`DraftBoardTable`'s `TierField`) is an editable input, not just a read-only badge, when a prep board is attached — typing a number stores it as your override for that player; typing the algorithm's own number back clears it, same "matching value carries no information" convention as the rest of the board. Scoped 1-20, independent of how many tiers the algorithm actually produced for that position, since the point is to let a user split or merge what the algorithm computed, not just relabel it. `TargetList`'s tier grouping reads the same override (`prep.entry(id).custom_tier ?? p.tier`) so the board and the targets panel never disagree about which tier a player is in. The read-only `TierBadge` (league Draft tab, no `prep`) is unaffected — overrides are personal-board-only, same as every other prep field.
  - **Reordering** rewrites the whole board in one `PUT .../order`: a rank only means something relative to every other player, so a partial write would leave the board inconsistent. The ↑/↓ buttons pass the *visible* neighbour rather than a direction, because with a position filter on, "move above the row above me" is the only reading that matches what you see. Server-side it's a single `unnest(...) WITH ORDINALITY` upsert — a few hundred round trips per nudge would make the board feel like work. Ranks are cleared first, so players dropped from the order don't keep a stale position; their tags and notes survive.
  - A row with no interest, no rank, no note **and no planned cost** carries no information, so that combination **deletes** the row (client and server agree on this).
  - **Plan panel** (`components/TeamPanel.tsx` + `TargetList.tsx` + `lib/roster.ts`) — **two modes**, persisted in `localStorage`: **Team** (the roster your planned prices add up to) and **Targets** (just the players you're chasing, grouped by position then tier — position first because tiers are computed within position, so a bare "Tier 2" spanning RBs and WRs would compare two different things; each tier shows how many targets are left in it, which is what tells you whether you can wait). Team mode: the players you've given a planned cost, assembled into the league's roster. **Docked beside the board, not a separate view** — it's a running total of decisions made in the table, so you add a player on the left and watch the budget move on the right. On `lg` and up it is **fixed to the right edge of the window** (`top: var(--nav-height)` to `bottom: 0`, `z-30` — above the table's sticky header at `z-20`, below the nav at `z-40`) and scrolls independently, so a long roster never drags the board with it; the page reserves the width with a margin rather than laying the panel out, since a fixed element is out of flow. Collapsed it becomes a tab pinned to the same edge. Open/closed persists in `localStorage` — it's a preference, not a destination. Below `lg` there's nowhere to dock, so it stacks under the board as an ordinary block. It shows spent/remaining, **max bid** (`remaining − $1 × other empty spots`, since every spot costs at least a dollar), starter points and starter VOR, plus warnings for over-budget, unfillable budgets, overflow and empty starters. Assignment is greedy by projected points, filling the **most restrictive slots first** — fill FLEX first and your only kicker ends up benched with K empty. Not a global optimum (that's an assignment problem), but it matches the optimum in ordinary cases and stays predictable, which is worth more here. Costs are editable in the panel; the board's `+` seeds one at our auction value.
  - **Targets and avoids are marked on the row itself** — a left accent edge (`interestRowClass`) — rather than duplicated in a panel above the table. Notes are edited inline in the board's Note column. Class names for the edge are whole literals in a lookup map, because Tailwind scans source text and never sees a class built by concatenation.
  - **Bench (`BN`) is part of the slot vocabulary** but starts nobody — `ranking.ComputeReplacementLevels` skips it, so it cannot move a replacement level or a price. It's carried because roster size is what tells the builder how many $1 spots a budget still has to cover.
- **Draft settings panel** (rendered only on `/draft-prep`): `components/DraftSettingsPanel.tsx` + `hooks/useDraftSettings.ts` let you edit the league settings that drive draft values — teams, auction budget, scoring format, starting-lineup slots (QB/RB/WR/TE/FLEX/**SFLEX**/K/DEF), and the pointing system.
  - **Pointing system.** A "Pointing system" section lists point values for the 9 stat categories with per-game rate data (pass/rush/rec yards+TDs, receptions, FG made, PAT made — `SCORING_STATS`/`SCORING_LABELS`), pre-filled from the league's own scoring (FG's distance buckets collapse into one flat average — `useDraftSettings.ts`'s `toScoring`/backend's `defaultScoring`). Editing any value sets `scoringCustomized: true`, which is the only thing that puts `scoring=` on the request; merely changing Teams/Budget/Slots does **not** freeze scoring the way it does for those fields — the Scoring format toggle (or the league's real scoring) keeps governing until a point value is actually touched, so a league that later changes its scoring doesn't inherit a stale snapshot from an unrelated edit. Once customized, it replaces `format` entirely: `draft_values.go` prices **every** position (not just kickers) from `scoring.ProjectionToCanonicalTotals` + `ScoreWithModifiers`, the same rate-based math kickers always used, just with user weights. `scoring_format` reports `"custom"` in that case, since it's no longer one of league/ppr/half/standard. Consensus diffing still needs a ppr/half/standard bucket to compare against, so it's approximated from the custom reception point value the same way the league's own scoring is classified.
  - **The draft math stays on the server.** Saving sends the settings as `draft-values` query overrides and the handler rescores; there is no second implementation in the frontend. `draftQuery(season, settings|null)` builds both the request params and the cache key, and is shared by DraftTab and `useKeepers` so the keeper tab's auction column agrees with the board.
  - **Edit → Save.** Edits are held as an unsaved draft (`editing`) so a half-typed roster never triggers a rescore; `save()` applies and persists them. `settings` is what the board is fetched with, `editing` is what the panel shows. The query uses `placeholderData: (prev) => prev`, so the old board stays on screen (with a "Rescoring…" note) while the new one loads.
  - **Persistence** is `localStorage` only — `fs.draft.settings.v1.{leagueId}` for settings, `fs.draft.position.v1.{leagueId}` for the position filter; nothing is stored server-side. `readSettings()` backfills keys added after a record was saved (SFLEX was), so older records can't leave an input uncontrolled. Overrides are sent **only once saved**: an uncustomized request carries no overrides, which is exactly what lets the response seed the panel without request and response chasing each other. "Reset" drops back to the league's real settings.
  - **Slot vocabulary.** The UI edits flat slot names; Yahoo expresses flex spots as eligibility lists (`W/R/T`, `Q/W/R/T`). `yahooToSlot`/`slotToYahoo` in `draft_values.go` translate between them; `ranking.ComputeStarterSlots` (an even split across eligible positions) exists only to check that translation round-trips (`TestSlotOverrideRoundTrip`) — it is **not** what determines replacement level or price. Flex shapes not modelled exactly (`W/R`) fold into FLEX. A `slots=` override that yields nothing startable is treated as absent rather than as an empty lineup, which would otherwise make every player worth his full point total.
  - **Flex/superflex replacement is pooled, not split evenly (`ranking.ComputeReplacementLevels`, fixed 2026-08-14).** An even split across a flex slot's eligible positions is a fine approximation for an ordinary FLEX (`W/R/T` — RB/WR/TE have comparable per-game value, so it's genuinely contested who claims the spot) but badly wrong for SFLEX (`Q/W/R/T`): QB so dramatically outscores the other three in points formats that a flat 1/4 split credited QB with only 0.25 of a starter slot per SFLEX, undervaluing QB by roughly half of what a real superflex market prices in. The fix: dedicated (single-position) slots claim starters directly (`count × numTeams`); each flex-type slot then pools its **still-unclaimed** eligible candidates by projected value and lets the actual best players claim the spots, greedily, position-blind. No tunable weight — in a normal PPR points league this naturally resolves to SFLEX going almost entirely to QB because the math says so, and ordinary FLEX still splits close to evenly because RB/WR/TE really are comparable at the margin. Verified live: a league with `QB:1,...,SFLEX:1` now prices QBs **identically** to an explicit `QB:2` (no SFLEX) roster — both land on 24 starters (threshold) / 2.0 slots-per-team, versus 15/1.25 under the old even split.
  - With an explicit `slots=` override the handler no longer needs Yahoo's roster settings, so a roster-settings fetch failure is non-fatal on that path.
- **My Team tab:** the league page's first tab (`?tab=my-team`), rendered only when the signed-in user owns a team in that league (`teams[].user_id`). It reuses `team-detail`'s `TeamPanel`, so the roster, matchup card and stat-period switcher are the same component as `/teams/:id` — that page is now just a header around the panel. A `?tab=my-team` URL falls back to Standings when the user owns no team here, but only *after* teams load, so deep links aren't bounced mid-fetch. `/leagues/{id}/my-team` is a stable alias that redirects to the tab.
- **Player Grades:** Three-layer separation: (1) Player Grade — real-life quality (0-100 percentile), computed in `cmd/projections/grades.go`; (2) Stat Projections — comp-based; (3) Fantasy League Value — VORP/z-scores. Grades use position-specific sub-score weights (production, efficiency, usage, durability). Computed via `make project-nfl ARGS="-grades"`. `nfl_player_grades` table stores results. Grade z-score (`overall_grade_z`) is injected into season profiles and used as a similarity dimension for comp matching (weight 1.25). Grade YoY trend applies a bounded ±5% adjustment to projected stats. Frontend: GradeCard on player detail, Grade column on projections/draft/players tabs, Grades view on `/statistics`. Former Rankings tab absorbed into Players tab; former standalone `/rankings` page absorbed into `/statistics` (`?view=grades`).
- **Yahoo decoupling:** Yahoo is the source of fantasy *context* (ownership, scoring categories, roster slots, FA status), not NFL *stats*. NFL season rankings pull raw stat values from `nfl_player_stats` via `services/nflstats.LoadSeasonStats` keyed on `gsis_id`, translating Yahoo stat IDs through `services/scoring.YahooToCanonical`. Yahoo-only fallbacks remain for: non-season stat types (lastweek, today), NBA leagues (no non-Yahoo stats feed yet), and live in-season keeper/roster views. See `.claude/plans/yahoo-decoupling.md`.
- **Native leagues (started 2026-08-19):** a league with no Yahoo league behind it at all — `leagues.source = 'native'`, `leagues.format` selects the season-rollover strategy (`redraft`|`keeper`|`dynasty`, migration 000021). `POST /api/leagues` creates one: name/sport/season/format + `settings` (num_teams, budget, slots, scoring, `draft_rounds` — the same vocabulary as the draft-values `?slots=&scoring=` overrides, plus how many rounds a generated draft class has) + `teams`, all in one transaction, owned by the creator (`leagues.user_id`). Mutations (`PUT .../settings`, team CRUD, rosters, picks, trades, rollover) are gated by `requireCommissioner()` (`leagues.go`) — native leagues only, `leagues.user_id` must match the caller; Yahoo leagues are never editable through this API. `services/leaguesettings` is the seam: a `Source` interface (`RosterPositions`, `ScoringMods`) with two implementations — `YahooSource` (live Yahoo settings/scoring calls) and `NativeSource` (reads the league's own `league_settings` JSONB row) — resolved per-request by `leagueSettingsSource()` (`handlers/yahoo_helpers.go`), so `draft_values.go` (and every future consumer) works identically regardless of where the league lives. Draft Prep and draft-values work against a native league the moment it's created. **No DEF slot** — nflverse has no team-defense `gsis_id`s, so `DEF` is rejected both when assigning a roster row (`league_rosters.go`'s `validSlots`) and when saving league settings (`validateNativeSlots`); kickers are unaffected (real `gsis_id`s exist). Rosters and contracts (migration 000022) live in `league_rosters`/`league_contracts` — every roster row carries a contract row (even $0), and a player belongs to at most one team per league by primary key, which is what the free-agent endpoint's `LEFT JOIN ... WHERE gsis_id IS NULL` relies on. Future draft picks and trades (migration 000023) live in `league_draft_picks`/`league_transactions` — see `draft_picks.go`/`trades.go`/`rollover.go` above; every mutation (assign, drop, trade, pick-use, rollover) logs a `league_transactions` row, which is what the frontend's activity log reads. **Frontend:** `/leagues/{id}?tab=roster` (`NativeRosterTab.tsx`, native-only tab) is the commissioner surface — team switcher (with a "Claim as my team" link on any unclaimed team — see below), cap-space summary, a shared `components/NativeRosterTable.tsx` (Player/Pos/**inline-editable Slot dropdown**/Salary/Years/Edit/Drop — the Slot dropdown *is* the weekly lineup-setting mechanism, there's no separate "Lineup" screen; it's what `ScoreLeagueWeek` reads), an "Assign player" flow (`components/PlayerAssignForm.tsx`, the first player-search UI in the app, shared with the pick-use flow), a two-team "Trade" builder (`components/TradeBuilder.tsx`, players + unused picks, scoped to a 2-team swap even though the backend supports N-team trades — that's what actually gets used), and a season-rollover confirm-and-execute control. The Draft tab's native-only "Picks" sub-section (`NativeDraftPicksTab.tsx`, replaces "Keepers" — that sub-tab is Yahoo-draft-result-shaped and 422s for a native league, same class of bug the Players tab had before `NativePlayersTab` existed) lists future picks by season/round and lets you generate a class or spend an unused pick. All of it goes through a hand-rolled `components/ui/dialog.tsx` (centered-modal counterpart to `mobile-sheet.tsx`, same no-animation convention) rather than a Radix dependency. `NativePlayersTab.tsx`'s Free Agents/Rostered tables show per-category projected stat columns (2026-08-20) — dynamic, not hardcoded: only the categories the league's own scoring weights nonzero come back from the API at all (`relevantPlayerStats` in `league_rosters.go`), so the column set adapts league to league, same "no hardcoded stat IDs" spirit as the existing Yahoo roster's dynamic columns (though the two pull from different vocabularies — Yahoo's live `stat_categories` vs. the native `leaguesettings.ScoringEditableStats` enum).

  **Weekly play (started 2026-08-20):** native leagues now get the same Standings/Scoreboard screens as Yahoo leagues, backed by `league_matchups` (migration 000024) instead of the Yahoo API. `NativeStandingsTab.tsx`/`NativeScoreboardTab.tsx` reuse the *same* `getLeagueStandings`/`getLeagueScoreboard` client calls Yahoo leagues use (the native/Yahoo branch is server-side); Scoreboard adds "Generate schedule" (shown once, when none exists for the season) and a "Score week N" commissioner control with Prev/Next week navigation. **Team claiming**: native teams start with no `user_id` at all (only Yahoo sync sets that) — `updateLeagueTeam(leagueId, teamId, {claim: true})` (wraps `PUT .../teams/{teamId}`, extended with an optional `Name`/`Claim` pair, only present fields change) assigns/releases `teams.user_id`, releasing any other team the same user already claimed in that league first (one team per user per league).

  **My Team + Roster merged into one page, "Roster" (2026-08-21).** They were the same content twice — a personal matchup-card-and-roster view, and a commissioner team-switcher-and-roster view. Native leagues have no separate My Team tab at all now (`league-detail/index.tsx` only renders `MyTeamTab` when `!isNative`); `NativeRosterTab.tsx` is the single combined page: a team switcher (defaults to your claimed team, same as My Team used to) sits alongside the commissioner tools (Claim/Trade/Assign/Rollover/activity log), and the matchup card + roster for whichever team is selected comes from `NativeTeamOverview` regardless of whose team it is. A `?tab=my-team` deep link (from the stable `/leagues/:id/my-team` redirect) resolves to the Roster tab for native leagues via `visibleTab` in `index.tsx`, ahead of the generic my-team-fallback logic Yahoo leagues use.

  **Explicit scope decisions, not gaps to silently fill later:** regular season only, no playoff bracket/seeding; scoring is a manual "Score week N" click after `make import-nfl`, never automatic/live (the underlying stats are a post-game batch import).

  **Team pages merged into Roster (2026-08-20, folded further 2026-08-21):** clicking a team name anywhere in a native league (Standings, Scoreboard matchup cards, Draft Picks' Owned-by/Original-owner columns, a Roster matchup card's opponent) is just a link to `/leagues/{leagueId}?tab=roster&team={teamId}` — there's no separate team page at all for native leagues, because there's no reader who isn't the commissioner in the single-user model, so a distinct "read-only view of someone else's team" was never a real requirement. `NativeRosterTab`'s selected team is derived straight from the `team` URL param (`teams.some(t => t.id === teamParam) ? teamParam : myTeam?.id ?? teams[0]?.id`, no `useState`/`useEffect` pair to keep in sync) — every link into the tab already carries the right team, and deriving instead of storing sidesteps the class of bug a `useState` initializer had here (`teams` loading after mount used to freeze the wrong default). `TeamDetailRouter` (`pages/TeamDetailRouter.tsx`) still exists at `/teams/:id` for Yahoo (branches to the Yahoo `TeamDetail` before its Yahoo-only hooks ever mount) but a native team there is just `<Navigate>`-redirected into the Roster tab, kept only for old bookmarks/stray links — nothing the app generates points at `/teams/:id` for a native team anymore. `components/NativeTeamOverview.tsx` (matchup card + `NativeRosterTable`, rendered by `NativeRosterTab` for whichever team is selected) lost its `readOnly` prop in the same change — `NativeTeamDetail.tsx`, the only caller that ever passed it, is deleted.

  **Full lineup shown even when empty (2026-08-21).** `NativeRosterTable` used to just list whatever players happened to be rostered — an unstarted team showed nothing at all. It now takes the league's `slots` config (from `getLeagueSettings`, threaded down through `NativeTeamOverview`) and renders one row per configured slot in `lib/nativeSlots.ts`'s new `SLOT_DISPLAY_ORDER` (starters first, then BN/TAXI/IR — distinct from `ROSTER_SLOTS`, which is just the `<select>`'s option order), padding with an inert "Empty" row for anything unfilled. A slot with more actual players than its configured count (e.g. after a settings change) still shows all of them — the padding is `Math.max(configured, actual)`, never a truncation.

  **Drag and drop for lineup slots (2026-08-21).** `NativeRosterTable` rows are draggable (native HTML5 DnD, no new dependency — `draggable` + `onDragStart`/`onDragOver`/`onDragLeave`/`onDrop`, payload is just the dragged player's `gsis_id` as `text/plain`) as a second way to change a player's slot, alongside the existing `<select>` dropdown, not replacing it — HTML5 DnD has no touch story, so the dropdown stays the one mechanism that always works on mobile/keyboard. Dropping onto an empty row reassigns the dragged player to that slot (one `updateLeagueRoster` call); dropping onto an occupied row swaps the two players' slots (two calls — the occupant takes the dragged player's old slot first, then the dragged player takes the target's), matching how drag-and-drop lineups behave elsewhere (Yahoo, ESPN) rather than silently doubling up a slot. A dragged-over row highlights (`bg-muted`) via `dragOverKey` state; both mutations invalidate the same roster/free-agent/transaction queries the dropdown already used. Verified live on league 42: drag-to-empty (RB → empty QB) and drag-to-occupied (swap two RBs' slots, confirmed via network tab that both PUTs fired) both work and survive a reload.

  **Pos styled as a button, team switcher as a dropdown (2026-08-21).** Two quick follow-ups to the changes above: the Pos cell (`NativeRosterTable`) now has an actual button chrome (`border-input`/`bg-background`, `hover:bg-accent`, matching the `outline` button variant) instead of looking like plain text, so it reads as clickable at a glance. Separately, `NativeRosterTab`'s team switcher went from a row of up to 12 `FilterChip` pills to a single `SelectControl` dropdown — same component `PlayerAssignForm`'s Team/Slot pickers already use, so a 12-team league doesn't need a wall of buttons to pick from.

  **Roster moved to the second tab (2026-08-21).** Dave asked for Roster to be the second tab in a native league. `league-detail/index.tsx`'s `TabsList`/`TabsContent` order is now Standings, Roster, Scoreboard, Players, Draft (Roster's `TabsTrigger`/`TabsContent` moved as a pair, still gated on `isNative`) — Yahoo leagues are unaffected since Roster never renders for them. Verified live: nav reads Standings/Roster/Scoreboard/Players/Draft and clicking Roster still shows the full team switcher + roster correctly.

  **Team avatars, placeholder for now (2026-08-21).** Dave asked for teams to get a "profile picture" like players have. There's no team-logo pipeline (native teams have no image field at all), so `TeamAvatar` (`components/ui/table-helpers.tsx`, alongside `PlayerAvatar`) renders a flat initials circle instead — same `bg-muted`/`shrink-0` treatment as `PlayerAvatar`'s no-image fallback, so it reads as the same kind of placeholder, not a new visual language. Every default-seeded team is literally named "Team N", and naive first-letter-of-each-word initialing collapses "Team 1" and "Team 10" to the same "T1" (one digit chopped off "10"); `teamInitials()` special-cases the `Team \d+` pattern to show the full number instead ("1" vs "10"), falling back to ordinary word-initials for any custom/renamed team. Wired into every place a team's identity is already shown as a named entity: `NativeStandingsTab`'s team column, `NativeScoreboardTab`'s matchup cards, `NativeTeamOverview`'s matchup card (both sides), and `NativeRosterTab`'s team switcher (next to the dropdown, since a native `<select>` can't render images inside its own options). Verified live: Team 1/Team 10 render distinctly everywhere; all 12 teams show correct distinct initials on the Standings page.

  **Picking button moved from Pos to Slot; Pos now lists all eligible slots (2026-08-21).** Dave asked for the clickable trigger to live in the Slot column (not Pos), and for Pos to instead show every slot the player is eligible for. `NativeRosterTable`'s Slot cell is now the button (same active/inactive chrome as before, just relocated) showing the player's current slot and toggling `pickingFor`; Pos is back to plain text, but instead of the player's bare real position it lists their full starter-eligible slot set via `SLOT_DISPLAY_ORDER.filter(s => !BENCH_SLOTS.has(s) && isSlotEligible(s, r.position)).join(' · ')` (e.g. an RB shows "RB · FLEX · SFLEX") — BN/TAXI/IR are excluded since every player is eligible there and it adds no information. Verified live: Slot shows as a bordered button per row, clicking it enters picking mode exactly as before (same row-highlighting behavior), and Pos correctly reads "RB · FLEX · SFLEX" for both rostered RBs.

  **Pos click replaced with clickable destination rows, not a dropdown (2026-08-21).** Dave asked for the floating eligible-slots menu (added minutes earlier) to go away in favor of the destination *rows themselves* being clickable and visually marked. Clicking Pos now just enters a "picking" mode (`pickingFor` state, the button's own chrome flips to the active/positive-accent look) — every row in the table that's a legal destination for that player lights up with the same left accent edge draft-prep uses for target/avoid rows (`border-l-[3px] border-l-primary`) and a pointer cursor, and clicking anywhere on that row (not just a sub-element) performs the move. This is strictly more correct than the dropdown it replaced: a slot *name* like "RB" doesn't disambiguate which physical RB row you mean when a league has two, but a specific row does — clicking an empty RB row assigns; clicking Jahmyr's occupied RB row swaps. Clicking outside the table (`mousedown` outside a table-wrapping ref) cancels picking with no side effects; clicking Pos again, or completing a move, also clears it. Verified live on league 42: picking an RB correctly lit up RB/FLEX/SFLEX/BN/TAXI/IR rows (not QB/WR/TE/K); clicking an empty FLEX row moved the player there; clicking an occupied RB row's Salary cell (not just Pos) correctly swapped both players; outside-click canceled cleanly.

  **Slot column made read-only, Pos click opens the slot picker (2026-08-21).** Dave asked for Slot to move to the first column as plain text, and for the eligible-slots picker to live behind clicking Pos instead of an always-visible `<select>`. `NativeRosterTable`'s column order is now Slot/Player/Pos/Salary/Years/actions; the old inline `<select>` is gone. Pos is now a button — clicking it opens a small absolutely-positioned menu (own hand-rolled markup, no new dependency — border + `bg-card`, no shadow, per the design system) listing that player's eligible slots (via the same `isSlotEligible` table), with the current slot shown but disabled. Only one row's menu is open at a time (`openSlotFor` state); a `mousedown` listener on `document` closes it on an outside click. The menu opens upward for rows in the bottom half of the table (`i > rows.length / 2`) so it doesn't get clipped by the scrollable table container. Drag-and-drop is unaffected — it's a row-level interaction, not tied to a specific cell. Verified live: Pos menu for an RB listed only BN/RB(disabled)/FLEX/SFLEX/TAXI/IR; clicking FLEX moved the player there immediately; clicking outside the open menu closed it with no side effects.

  **Lineup slot eligibility (2026-08-21).** A player can only occupy a slot their real position is eligible for — QB/RB/WR/TE/K slots take only that position, FLEX takes RB/WR/TE, SFLEX takes QB/RB/WR/TE, and BN/TAXI/IR take anyone (they're not starters, so there's no lineup-legality question). Enforced on both ends: backend `slotEligibleForPosition` (`league_rosters.go`) is checked in both `AssignLeagueRoster` and `UpdateLeagueRoster` whenever a slot is set, looking up the player's real `nfl_players.position` and rejecting a mismatch with 400 — this is the actual authority, matching every other native-league invariant (DEF rejection, double-rostering, etc.) being enforced server-side, not just assumed from the UI. Frontend mirrors it for UX (`isSlotEligible` in `lib/nativeSlots.ts`, same eligibility table): `NativeRosterTable`'s slot dropdown only lists eligible slots (plus the player's current slot, defensively, so the `<select>` is never left pointing at an option that isn't rendered); `PlayerAssignForm`'s slot picker filters the same way and resets to BN if the previously-picked slot doesn't fit a newly-selected player. Drag-and-drop gates both directions before the drop even fires: the dragged player must be eligible for the target slot, and — for a swap onto an occupied row — the occupant must be eligible for the slot being vacated; the check runs in `onDragOver` (via `draggingEntry`, tracked in component state since `DataTransfer.getData()` isn't readable until the actual `drop` event) and simply doesn't call `preventDefault()` when ineligible, which is what makes the browser show its native "no drop" cursor and never deliver a `drop` event at all — no separate error state needed. Verified live: backend curl confirmed a rejected RB→QB PUT (400, "RB is not eligible for slot QB") and an accepted RB→FLEX PUT; in the browser, the RB's dropdown only offered BN/RB/FLEX/SFLEX/TAXI/IR, a drag onto the empty QB row was silently refused (zero network requests), and a drag onto the empty FLEX row succeeded and persisted through a reload.

  **Design review implementation (2026-08-21).** A full design-review pass (spec published as an Artifact, then reviewed by a separate design session that returned an 8-item response + annotated mockup) was implemented wholesale across the native-league screens:
  - **New `--format` token + `Badge variant="format"`** (`index.css`, `badge.tsx`): the league-format badge (dynasty/keeper/redraft) was misusing `variant="purple"`, which `badge.tsx`'s own source comment reserves for projected/future data. A dedicated green hue (`hsl(150 * * *)`, both themes) replaces it everywhere the format shows — league header (`index.tsx`, now with a `· N teams` count in the subtitle too) and the Home league list.
  - **`TeamAvatar` recolored to match `LeagueMark`'s convention** (`table-helpers.tsx`): was a neutral `bg-muted` circle; now cycles the same three accent hues (`bg-primary`/`bg-secondary`/`bg-highlight`) as Home's league squares, picked by a deterministic hash of the team name (not array index, since a team's color has to agree across Standings/Scoreboard/the team switcher, which don't share one array order) — square for leagues, circle for teams is the type distinction. Two placeholder conventions closing into one.
  - **Standings gets an L5 win/loss strip and a playoff cutoff line** (`scoring.go`'s `nativeStandings` + `NativeStandingsTab.tsx`): `standingResp` gained `last5 []string` (chronological W/L/T over the matchup-history query, now `ORDER BY week ASC`) and `makes_playoffs bool`. There's no real playoff-teams setting, so the cutoff is a size heuristic (`playoffCutoff`: 2 of ≤4, 4 of ≤8, else half) — flagged as an approximation, not a real bracket (still explicitly out of scope). The cutoff row gets a dashed pink `border-bottom`; the signed-in user's own team gets a `bg-muted` tint + a quiet "you" label instead of an accent color, which would misread as "winning."
  - **Scoreboard: two-column grid, real projected points, winner treatment** (`schedule.go`'s new `nativeProjectedPoints` + `scoring.go`'s `nativeScoreboard` + `NativeScoreboardTab.tsx`): an unscored matchup now shows each team's actual projected points (one game's worth of `nfl_projections` rates, scored through the league's own modifiers — same pipeline `ScoreLeagueWeek`/`relevantPlayerStats` use, just `ProjectionToCanonicalTotals(rates, 1)` instead of a full season) instead of a blank dash. Copy changed "Not yet scored" → "Awaiting scoring · week N" (no fabricated calendar date — native leagues have no season-start-date setting to compute one from, a deliberate deviation from the mockup's literal date string). A scored card's winner gets a tinted header row, bold name + score, and a small solid "W" pill (`Badge` default variant, which is already the system's one solid fill).
  - **Roster table rewrite** (`NativeRosterTable.tsx`): Salary+Years merged into one **Contract** column showing years *remaining* (`$74 · 2 yrs left` / `$9 · final yr` / `$18 · Y2Y`) via `contractLabel()`. Edit/Drop text links collapsed into a single `⋯` icon menu (`RowActionsMenu`, own hand-rolled popover, closes on outside click, swaps to "Confirm drop" in place rather than opening a second control). Rows are grouped into **Starters / Bench / Taxi & IR** with a fill-count + total-salary header each (`groupOf`/`GROUP_LABEL`), and every row gets a leading `⠿` drag-handle glyph. Picking mode's banner moved from a floating callout to a plain row inside the table, right above Starters, `aria-live="polite"`. The design review's own follow-up (open item 4) walked back an earlier plan to drop per-row text and instead made it the **highest-priority fix**: every highlighted destination row now also carries a "place here"/"swap here" label (in the empty-row cell, or via `PlayerCell`'s `sub` prop for an occupied one) plus `role="button"`/`tabIndex`/`aria-label`/`onKeyDown`, since color and cursor alone don't exist for a screen reader. **Deliberate deviation:** the review flagged (and separately contradicted itself over) whether the Eligible/Pos column should truncate to just the player's true position — kept the existing full eligible-slot list instead, since truncating was explicitly flagged as a possible regression of "see every legal destination" and the review's own light-theme mockup showed the full list, not the truncated one.
  - **Players tab: multi-select position filter, tray/chip rule, L4 sparkline** (`NativePlayersTab.tsx` + new `weeklyTrend` helper in `league_rosters.go`): position filtering is now multi-select (checkmarked chips, several active at once) instead of single-select "All/QB/RB…". The Free Agents/Rostered toggle became a filled tray (matching Draft's Values/Picks switcher) instead of chips, per the review's codified rule: **a tray switches which dataset you're looking at, chips filter the one you're in** — Free Agents vs. Rostered changes the whole dataset, so it should never have been chips. A new **L4 wks** column (`L4Sparkline`, plain inline SVG, no charting library) shows each player's real fantasy points (this league's own scoring, via `weeklyTrend` — the trailing up to 4 weeks with any imported `nfl_player_stats`, independent of whether this league has actually run `ScoreLeagueWeek` for them) — pink stroke trending up, negative-blue down, muted flat, same semantic colors as the Standings L5 strip. **Deliberate scope cut:** the review's "Season Total/Per Game/L4/L8 time-window dropdown" (which would toggle every stat column's aggregation) was not built — it would need real backend support for each window, and a control that only pretends to switch data is worse than no control; only the always-real L4 sparkline shipped.
  - **Trade dialog gets a review step** (`TradeBuilder.tsx`): "Execute trade" replaced with "Review trade →" — clicking swaps the dialog body for a plain summary of each side's outgoing assets and resulting cap (colored negative when a side goes over budget), with Back/Confirm controls, before the mutation actually fires. Reasoning from the review: a trade has no real undo (re-trading back re-executes contracts and leaves two activity-log rows that misdescribe what happened), so it was the one irreversible action with no gate at all.
  - **Home league list: format badge + sync-status tag** (`Home.tsx`): a native league's row gets the same green format badge as the league header; a Yahoo-synced league's row gets a quiet `⟳ Yahoo` tag in the same right-aligned slot a native league's `Native` tag occupies — added specifically so a broken/stale Yahoo sync (the app's own March-2026 API breakage) has an at-a-glance marker on the list, not just something you discover after opening the league. `LeagueMark`'s existing colored-square convention was explicitly reconsidered and kept (not unified downward to neutral gray) — only `TeamAvatar` moved to match it.
  - **Roster count now gets status color** (`NativeRosterTab.tsx`): the cap-summary strip's Roster count (e.g. `14/16`) turns negative-blue when it exceeds the league's configured total slots, matching Remaining's existing negative-below-zero treatment — color only on the two cells that can actually be invalid, no progress bar (a bar would imply a smooth budget where the real state is binary).
  - Verified live end-to-end on league 42: Standings' L5/playoff line/tint, Roster's grouped-table drag+pick+contract+menu (including an actual FLEX move), Scoreboard's two-column grid + real projected points + a scored week, Players' multi-select filters + tray, and the Trade review screen's cap math all rendered and behaved correctly with no console errors.

  Not yet wired: `analysis.go` rankings (still Yahoo-only), `league_players.go` native player search, native keeper-format rollover (501 — no native keeper-designation flow exists), draft order/`overall_pick` (no real standings-based seeding, picks are unordered within a round) — see `.claude/plans/native-leagues.md` for the phased plan and open decisions.
- **SQL approach:** handlers use raw pgx queries for all database access. This is the established pattern — do NOT introduce sqlc or an ORM. `sqlc.yaml` exists but is unused; raw queries are preferred for their directness and flexibility with pgx features (e.g. `ANY($1)` with slices). Keep queries in handler methods, not in a separate query layer.
- **New resource checklist:** model → migration → handler → route → yahoo method (if needed) → TS interface → API function → query key → page → **update docs**
